# MIYRAA – Face Protection Protocol (FPP)

## Setup

1. Create Firebase project
2. Download service account → firebase_service_account.json
3. Set env vars:
   - FPP_ADMIN_API_KEY
   - FPP_EMBED_KEY
4. Install deps:
   pip install -r requirements.txt
5. Run:
   python app.py
